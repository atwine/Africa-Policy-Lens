# requirements.txt
# PolicyLens — African Data Protection Policy Agentic RAG Tool

# Core framework
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=1.3.3      # LLM: vLLM OpenAI-compatible endpoint (Llama 3.3 70B)
langchain-ollama>=0.3.0      # Embeddings only: nomic-embed-text (local Ollama)
langchain-chroma>=0.2.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0

# Document processing
pdfplumber>=0.11.0

# Vector store
chromadb>=0.5.0

# Frontend
streamlit>=1.38.0
markdown>=3.4.0              # Converts LLM markdown → HTML for answer card rendering

# HTTP client for downloading source documents
requests>=2.31.0

# Utilities
numpy>=1.24.0
